{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",

  "// AzureAd": "MCP OAuth 2.1 protected-resource auth (Auth/McpAuthOptions). Values are supplied by the host stack at deploy time (env: AzureAd__TenantId, AzureAd__Audience, AzureAd__OwnerOid).",
  "AzureAd": {
    "TenantId": "",
    "Audience": "api://pdp-mcp",
    "OwnerOid": ""
  },

  "// ManagedIdentity": "Client id of uami-mcp; when set the host uses ManagedIdentityCredential (Azure) and the Entra-token Postgres path. Empty locally → DefaultAzureCredential + plain connection string.",
  "ManagedIdentity": {
    "ClientId": ""
  },

  "// ControlPlane": "Bound by the spec-006 verb layer. PostgresConnectionString carries Host/Database/Username (the uami-mcp display name) with NO password in Azure; ApplicationInsightsConnectionString is set from APPLICATIONINSIGHTS_CONNECTION_STRING at startup.",
  "ControlPlane": {
    "PostgresConnectionString": ""
  }
}
