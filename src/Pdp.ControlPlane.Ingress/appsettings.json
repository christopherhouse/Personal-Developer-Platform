{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning",
      "Yarp": "Information"
    }
  },
  "AllowedHosts": "*",

  "//": "The ONE public surface (Article IX): forward only POST /webhooks/github to the internal Api;",
  "// ": "everything else is unmatched and 404s. The destination Address is overridden by the Aspire",
  "//  ": "AppHost (api.GetEndpoint) for local dev and by spec-007 ACA config in production.",
  "ReverseProxy": {
    "Routes": {
      "github-webhook": {
        "ClusterId": "control-plane-api",
        "Match": {
          "Path": "/webhooks/github",
          "Methods": [ "POST" ]
        }
      }
    },
    "Clusters": {
      "control-plane-api": {
        "Destinations": {
          "api": {
            "Address": "http://localhost:5080/"
          }
        }
      }
    }
  }
}
