# Pdp.AppHost

Control plane (spec 006). The .NET Aspire AppHost for **local dev** — Postgres container +
`Pdp.ControlPlane.Api` + `Pdp.ControlPlane.Ingress` in one F5. Dev-only orchestration; production ACA
deployment is **spec 007**.
