# Outputs land with their resources (T023): ACR login server, container-app FQDNs, the per-app UAMI
# names/object-ids/client-ids, and — critically — the one-time `pgaadauth_create_principal_with_oid`
# psql command(s) the owner runs to register uami-api / uami-mcp as Postgres principals (research §6,
# SC-010 single reviewed manual step). Phase 1 (Setup) declares the file only.
